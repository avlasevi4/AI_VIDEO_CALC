# Vendored dependency

`supabase.min.js` — browser build of `@supabase/supabase-js` 2.112.3 downloaded from jsDelivr/npm. It is pinned locally so authentication can initialize without a runtime CDN dependency.
